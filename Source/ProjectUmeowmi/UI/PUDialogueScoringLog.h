#pragma once

#include "CoreMinimal.h"

/** Output Log filter: search for [PUDialogueScoring] (dish scoring + dialogue viewport swap / refresh). */
namespace PUDialogueScoringLog
{
	inline constexpr const TCHAR* Tag = TEXT("[PUDialogueScoring]");
}
